import { beforeAll, afterAll, beforeEach } from 'vitest';

beforeAll(async () => {
  // global bootstrap if needed
});

beforeEach(() => {
  // reset globals/mocks if needed
});

afterAll(async () => {
  // global teardown if needed
});
