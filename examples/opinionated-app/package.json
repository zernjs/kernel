{
  "name": "opinionated-app",
  "version": "1.0.0",
  "private": true,
  "description": "Opinionated Zern Kernel application boilerplate",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zern/kernel": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.4",
    "typescript": "^5.7.3"
  }
}
